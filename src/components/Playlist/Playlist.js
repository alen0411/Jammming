import React from "react";

import "./Playlist.css";

const Playlist = () => {
    return (
        <div>
            <playlist />
            <button className="saveToSpotify">Save to Spotify</button>
        </div>
    );
};

export default Playlist;